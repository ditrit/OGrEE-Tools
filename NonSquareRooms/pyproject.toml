[tool.poetry]
name = "non_square_rooms"
version = "0.1.0"
description = ""
authors = ["None"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = ">=1.24.1"
shapely = ">=2.0.0"
matplotlib = ">=3.7.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
